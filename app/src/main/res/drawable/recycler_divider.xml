<?xml version="1.0" encoding="utf-8"?>
<!-- 画一个矩形 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">

    <!-- 渐变色 -->
    <gradient
        android:centerColor="@color/c0"
        android:endColor="@color/c0"
        android:startColor="@color/c0"
        android:type="linear"/>
    <!--设置-->
    <size
        android:width="5dp"
        android:height="5dp"/>

</shape>